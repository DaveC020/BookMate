console.log("Dashboard loaded");

// Example: Add interactivity later (like clicking a book card)
document.querySelectorAll(".book-card").forEach(card => {
  card.addEventListener("click", () => {
    alert("Book clicked!");
  });
});
